name: examen2
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.5.0
  cloud_firestore: 5.4.2
  provider: ^6.0.0 # <-- Añade la dependencia de provider
  go_router: ^6.0.0  # <-- Añade la dependencia de go_router



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
